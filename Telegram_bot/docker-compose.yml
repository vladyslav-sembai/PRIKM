---
version: '3.3'

services:
  course_valute:
    image: cours-tg_bot:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    container_name: valute-tg_bot
    environment:
      MY_TELEGRAM_BOT_TOKEN: ${MY_TELEGRAM_BOT_TOKEN}